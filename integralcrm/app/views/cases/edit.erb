<div class="w3-main" style="margin-left:40px;margin-top:43px;">

  <!-- Header -->
  <header class="w3-container" style="padding-top:22px">
    <h5><b><i class="fa fa-file"></i> Edit Case</b></h5>
  </header>

<div class="w3-row-padding w3-responsive" style="overflow-x:auto">
    <form action="/cases/<%=@case.id%>" method="post">
    <input type="hidden" name="_method" value="patch">
    
    <label class="w3-panel">Status: </label>
    <select name="kase[status]" value="<%=@case.status%>">
        <option selected="selected" value="Open">Open</option>
        <option value="Closed">Closed</option>
        <option value="Open">N/A</option>
    </select>

    <label class="w3-panel">Open Date: </label>
    <input type="date" name="kase[open_date]" value=<%=@case.open_date%>>

    <label class="w3-panel">Close Date: </label>
    <input type="date" name="kase[close_date]" value=<%= @case.close_date%>>


    <label class="w3-panel">Additional Info: </label>
    <textarea name="kase[add_info]"><%=@case.add_info%></textarea>
<br>
    <div class="w3-padding w3-third">

    <label class="w3-panel">Owner: </label>
      <select size="5" name="kase[owner]">
        <option value="<%=@current_user.id%>" selected="selected"><%=@current_user.username%></option>
        <%@users.each do |user|%>
            <option value="<%=user.id%>"><%=user.username%></option>
        <%end%>
    </select>

    </div>

    <div class="w3-bar">

    <button class="w3-button w3-yellow w3-padding-large w3-large w3-margin-top" type="submit">
    Update Case
    </button>

    </div>
    </form>

</div>

</div>